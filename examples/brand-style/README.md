## brand-style Preset 

This example demonstrates how a single JSON preset is reused across:
- Single generation
- Batch generation

All images were generated using the same style JSON with different prompts.
